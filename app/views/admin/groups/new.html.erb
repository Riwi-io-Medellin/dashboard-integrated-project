<% content_for(:title, "Nuevo Grupo — RIWI") %>

<div class="max-w-lg">
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-white">Nuevo Grupo</h1>
    <p class="text-gray-500 mt-1">Crea un nuevo grupo de formación</p>
  </div>

  <%= render "form", group: @group %>
</div>
