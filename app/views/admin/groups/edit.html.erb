<% content_for(:title, "Editar Grupo — RIWI") %>

<div class="max-w-lg">
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-white">Editar Grupo</h1>
    <p class="text-gray-500 mt-1">Modifica el grupo "<%= @group.name %>"</p>
  </div>

  <%= render "form", group: @group %>
</div>
